# Flux 2 Flex — 1X1
platform: replicate
endpoint: black-forest-labs/flux-2-flex
media_type: image
parameters:
  aspect_ratio: "1:1"
  output_format: "png"
  num_outputs: 1
pricing:
  base_cost: 0.1
  currency: USD

# Prompt modifications (uncomment to use)
#prompt_prefix: ''
#prompt_suffix: ''
