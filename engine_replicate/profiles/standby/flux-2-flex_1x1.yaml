# Flux 2 Flex — square, webp
platform: replicate
endpoint: black-forest-labs/flux-2-flex
media_type: image
parameters:
  aspect_ratio: "1:1"
  output_format: "webp"
pricing:
  base_cost: 0.1
  currency: "USD"
